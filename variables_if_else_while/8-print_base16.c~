#include <stdio.h>
/**
* main - Entry point of the program
*
* Return: Always 0 (success)
*/
int main(void)
{
char num = '0';
for (num = '0'; num <= 15; num++)
{
putchar("%d", num);
}
putchar("\n");
return (0);
}
