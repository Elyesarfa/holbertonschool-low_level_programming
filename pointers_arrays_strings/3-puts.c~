#include "main.h"
/**
* _puts - Prints a string followed by a new line.
* @str: Pointer to a string to be printed.
*/
void _puts(char *str)
{
while (*str != '\0');
{
_puts(*str);
str++;
}
_puts('\n');
}
